{
  "name": "algorithms",
  "packageManager": "yarn@3.2.4",
  "scripts": {
    "test": "vitest run",
    "test:sort": "vitest run sort.test.ts --reporter verbose",
    "test:binary-search": "vitest run binary-search.test.ts --reporter verbose",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "vitest": "^0.25.1"
  },
  "devDependencies": {}
}
